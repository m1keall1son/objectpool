#include "catch.hpp"
#include "../ObjectPool.hpp"
//#include "test_common.h"

TEST_CASE( "Create and destroy an object pool", "[ObjectPool]" ) {        
      /*  bool result = true;
        try{
            auto pool = ObjectPool<Test>();
        }catch(...){
            result = false;
        }
        REQUIRE(result == true);*/
}
    